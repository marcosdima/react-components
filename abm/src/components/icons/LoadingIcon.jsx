import { LoadingContainer, SpinnerIcon } from '../../styles/icons/Loading.styles';

const LoadingIcon = () => (
  <LoadingContainer>
    <SpinnerIcon />
  </LoadingContainer>
);

export default LoadingIcon;
