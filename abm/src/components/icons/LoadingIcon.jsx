import { LoadingContainer, SpinnerIcon } from '../../styles/Loading.styles';

const LoadingIcon = () => (
  <LoadingContainer>
    <SpinnerIcon />
  </LoadingContainer>
);

export default LoadingIcon;
