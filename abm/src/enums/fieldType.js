const fieldType = Object.freeze({
    CHAR: 'char',
    EMAIL: 'email',
    NUMBER: 'number',
    IMAGE: 'image',
    PASSWORD: 'password',
    SELECTOR: 'selector',
    TEXT_AREA: 'text_area',
    SEARCH_LOCATION: 'search_location',
});

export default fieldType;
